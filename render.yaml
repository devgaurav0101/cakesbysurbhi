services:
  - type: web
    name: cakes-by-surbhi
    runtime: static
    plan: free
    buildCommand: 'true'
    staticPublishPath: .
    autoDeployTrigger: commit
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: true
